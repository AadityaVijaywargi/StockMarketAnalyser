"""
Trainer Placeholder.
Fits models on built datasets.
"""
class MLTrainer:
    def __init__(self, config: dict):
        self.config = config

    def train(self, train_data, val_data) -> str:
        """Trains an ML model and returns the path to the saved model."""
        return ""
